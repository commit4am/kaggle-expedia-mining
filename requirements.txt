# Learning-to-rank pipeline. Floors chosen so the 2019 notebooks run on a
# current Python; XGBoost 2.x changed XGBRanker defaults, so it is capped.
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
xgboost>=1.7,<2.0
imbalanced-learn>=0.11
matplotlib>=3.7
seaborn>=0.12
notebook>=7.0
